// #include "fixed-layer-norm.h"

// FixArray mean(const vector<FixArray> &input, const FixOp &fixop, size_t row, size_t column)
// {
//     int N = input.size();
//     int n = input[0].size;
//     int ell = input[0].ell;
//     int s = input[0].s;
//     bool signed_ = input[0].signed_;

//     // BoolArray all_0 = bool_op->input(ALICE, N * n, uint8_t(0));
//     // BoolArray all_1 = bool_op->input(ALICE, N * n, 1);

//     // FixArray sum = fixop.tree_sum(input);
//     FixArray sum;
//     return sum;
// }